#@ load("@ytt:data", "data")
#@ load("@lib:rest_block.star", "rest_block")

version: #@ data.values.version

api:
#@ for i in range(1,6):
#@ url = "api" + "{}".format(i) + "_url"
#@ method = "api" + "{}".format(i) + "_method"
#@ description = "api" + "{}".format(i) + "_description"
#@ if data.values[url] != "":
- #@ rest_block(data.values[url],data.values[method],data.values[description])
#@ end
#@ end

advanced: 
  param1: #@ data.values.adv_param1
  param2: #@ data.values.adv_param2
